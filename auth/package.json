{
  "name": "auth",
  "version": "1.0.0",
  "scripts": {
    "compile": "cp ../tsconfig.json.template ./tsconfig.json && tsc"
  },
  "license": "Apache 2.0",
  "dependencies": {
    "firebase": "^11.0.2",
    "firebase-admin": "^12.0.0",
    "firebaseui": "^5.0.0"
  }
}
